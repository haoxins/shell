#!/usr/bin/env node

'use strict'

const cp = require('child_process')
const { join } = require('path')
const fs = require('fs')

const MODE_HX = 'coderhaoxin'
const MODE_GO = 'golang'

let mode = 'normal'

let params = process.argv.slice(2)

if (params[0] === '-go') {
  mode = MODE_GO
  params.shift()
} else if (params[0] === '-hx') {
  mode = MODE_HX
  params.shift()
}

if (params.length !== 1) {
  quit()
}

if (!params[0].includes('/')) {
  quit()
}

params = params[0].split('/')
if (params.length !== 2) {
  quit()
}

let dir = params[0]
let repo = params[1]

let extra = ''
if (mode === MODE_HX) {
  extra = '-coderhaoxin'
}

let dest = `git@github.com${extra}:${dir}/${repo}.git`
console.log('clone: %s', dest)

const GOPATH = join(process.env.GOPATH, 'src/github.com')
const GITHUBDIR = process.env.GITHUBDIR

if (mode === MODE_GO) {
  console.info('go to: %s', GOPATH)
  process.chdir(GOPATH)
} else if (GITHUBDIR) {
  console.info('go to: %s', GITHUBDIR)
  process.chdir(GITHUBDIR)
}

try {
  fs.accessSync('./' + dir)
  console.info('%s exists', dir)
} catch (e) {
  fs.mkdirSync(dir)
}

process.chdir('./' + dir)

try {
  fs.accessSync('./' + repo)
  console.info('%s/%s exists', dir, repo)
} catch (e) {
  clone()
}

function clone() {
  let result = cp.spawnSync('git', ['clone', dest])

  if (result.status) {
    console.error(result.stderr.toString())
  } else {
    console.info(result.stdout.toString())
  }
}

function quit() {
  console.error('invalid params, quit')
  console.info('usage: gitclone [-go|-hx] owner/repo')
  process.exit(1)
}
