#!/usr/bin/env bash

. util/kit.sh

loglog 'all remotes:'
git remote

loglog 'all branches:'
git branch

loglog 'latest 5 tags:'
git tag | tail -5

loglog 'stash list:'
git stash list

loglog 'repo status:'
git status
